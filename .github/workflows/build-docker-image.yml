name: Construir imagen y subir a DockerHub

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Login en Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.TOKEN }}
          password: ${{ secrets.TOKEN2 }}

      - name: Preparar construcción con Docker Buildx
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.TOKEN }}/docker-github-actions:latest
            ${{ secrets.TOKEN }}/docker-github-actions:${{ github.sha }}

